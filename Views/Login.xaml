<Window x:Class="BananaPay.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BananaPay"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="BotaoRedondo" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#EAB01E"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#EAB01E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#F1CA68"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#F1CA68"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Border x:Name="CadastroCanto" BorderThickness="1" HorizontalAlignment="Left" Height="434" VerticalAlignment="Center" Width="224" Background="#FF45EA93">
            <Button Style="{DynamicResource BotaoRedondo}" x:Name="BotaoIrCadastro" Content="Cadastrar" HorizontalAlignment="Center" Height="44" VerticalAlignment="Top" Width="132" Margin="0,248,0,0" Click="IrCadastro_Click" Background="#FF45EA93" Foreground="White" FontWeight="Bold" FontSize="16" RenderTransformOrigin="0.5,0.5" />
        </Border>
        <Label Content="Faça parte do nosso banco e crie&#xA;&#x9;sua conta agora" Margin="6,178,0,0" RenderTransformOrigin="0.28,0.492" Foreground="White" FontSize="14" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Label Content="Cadastre-se" Margin="42,124,0,0" RenderTransformOrigin="0.28,0.492" Foreground="White" FontSize="24" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
        <Label Content="Entrar no banco" HorizontalAlignment="Left" Height="63" Margin="400,65,0,0" VerticalAlignment="Top" Width="196" Foreground="#FF45EA93" FontWeight="Bold" FontSize="24"/>
        <TextBox x:Name="CaixaLoginCPF" HorizontalAlignment="Left" Height="33" Margin="400,133,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="199"/>
        <TextBox x:Name="CaixaLoginSenha" HorizontalAlignment="Left" Height="33" Margin="400,192,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="199" Foreground="Black"/>
        <Button x:Name="BotaoLogin" Content="Login" HorizontalAlignment="Left" Height="45" Margin="422,266,0,0" VerticalAlignment="Top" Width="156" Background="#FF45EA93" FontWeight="Bold" FontSize="20" Style="{DynamicResource BotaoRedondo}" BorderBrush="White" Foreground="White" Click="BotaoLogin_Click"/>
        <Label Content="CPF:" HorizontalAlignment="Left" Height="27" Margin="366,136,0,0" VerticalAlignment="Top" Width="46" FontSize="14"/>
        <Label Content="Senha:" HorizontalAlignment="Left" Height="30" Margin="352,194,0,0" VerticalAlignment="Top" Width="56" FontSize="14"/>

    </Grid>
</Window>

